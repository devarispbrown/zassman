$boxes = $('<%= j render(@assets) %>')

$('#assets').append( $boxes ).imagesLoaded( function(){
    $('#assets').masonry( 'reload');
});
<% if @assets.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate(@assets) %>');
<% else %>
    $('.pagination').remove();
<% end %>